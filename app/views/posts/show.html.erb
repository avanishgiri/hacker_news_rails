<%= render "post", :post => @post %>
<% if @post.user == session[:current_user] %>
  <%= link_to "Destroy", post_path(@post) , :method => :delete %>
  <%= link_to "Edit", edit_post_path(@post) %>
<% end %>

<ul>
    <%= render :partial => "comment", :collection => @post.comments %>
</ul>
